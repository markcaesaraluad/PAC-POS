# PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE
# Copy this to .env.production and update with actual production values

# Environment
NODE_ENV=production
ENVIRONMENT=production

# Database
MONGO_URL=mongodb://localhost:27017/pos_system

# Authentication
SECRET_KEY=CHANGE-THIS-TO-SECURE-RANDOM-STRING-FOR-PRODUCTION
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration (CRITICAL FOR PRODUCTION)
# Replace with your actual production frontend domain(s)
CORS_ALLOWED_ORIGINS=https://your-production-frontend-domain.com,https://www.your-production-frontend-domain.com

# Proxy Configuration (set to true if behind reverse proxy/load balancer)
TRUST_PROXY=true

# Cookie Settings for HTTPS Production
COOKIE_SECURE=true
COOKIE_SAMESITE=None
COOKIE_DOMAIN=.your-production-domain.com

# Backend URL that frontend will use
FRONTEND_BACKEND_URL=https://your-production-backend-domain.com

# Example for typical production setup:
# CORS_ALLOWED_ORIGINS=https://pos.yourcompany.com,https://www.pos.yourcompany.com
# COOKIE_DOMAIN=.yourcompany.com
# FRONTEND_BACKEND_URL=https://api.pos.yourcompany.com