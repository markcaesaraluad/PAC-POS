# PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE FOR FRONTEND
# Copy this to .env.production and update with actual production values

# Backend API URL (CRITICAL - must match your production backend)
REACT_APP_BACKEND_URL=https://your-production-backend-domain.com

# Build configuration
NODE_ENV=production
GENERATE_SOURCEMAP=false

# Security
DANGEROUSLY_DISABLE_HOST_CHECK=false

# Example for typical production setup:
# REACT_APP_BACKEND_URL=https://api.pos.yourcompany.com